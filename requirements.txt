# 基础依赖（CPU 模式即可运行；精确版本 = 已验证环境）
# NVIDIA 显卡用户追加安装：pip install -r requirements-gpu.txt
# 一键复现完整已验证环境（含 GPU 库）用 requirements.lock
pyaudiowpatch==0.2.12.8
numpy==2.5.3
onnxruntime==1.30.0
faster-whisper==1.2.1
openai==3.16.2
fastapi==0.141.1
uvicorn==0.53.0
python-multipart==0.0.32
pypdf==6.19.0
python-docx==1.2.0
tomli_w==1.2.0
pywebview==6.2.1
